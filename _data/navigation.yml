# The following is the order of the links in the header of the website.
#
# The top navigation bar (masthead) is currently not displayed, keeping the
# landing page clean. If you ever re-enable it, these links will be used.

main:
  - title: "Publications"
    url: /publications/
